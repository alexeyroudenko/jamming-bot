# Mood + palette prototype moved to the **mood-service** image.
# See repository root: mood-service/ (POST /api/v1/mood/snapshot/).
# Do not add torch/transformers imports here — keep app-service slim.
